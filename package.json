{
  "name": "alexa-github-wrapper",
  "scripts": {
    "setup": "ask setup",
    "deploy": "ask deploy",
    "setup-storage": "node setupStorage.js"
  },
  "dependencies": {
    "aws-sdk": "^2.224.1"
  }
}
